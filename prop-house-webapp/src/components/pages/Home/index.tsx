import { Container } from 'react-bootstrap';
import HomeHeader from '../../HomeHeader';

const Home = () => {
  return (
    <Container>
      <HomeHeader />
    </Container>
  );
};

export default Home;
