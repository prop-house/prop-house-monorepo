import { Strategy } from '../../types/Strategy';

export const aiPod: Strategy = async (
  userAddress,
  communityAddress,
  blockTag,
  provider,
  roundId,
) => {
  // implement logic for ai pod
  return 666;
};
